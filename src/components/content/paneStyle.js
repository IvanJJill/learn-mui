const styles = theme => ({
    paper: {
        height: 500,
        padding: 20,
        overflowY: "auto",
        marginTop: "1em"
    },

    welcomeBody: {
        marginTop: 20,
    }
})

export default styles;  