const styles = theme => ({
    paper: {
        height: 500,
        padding: 20,
        marginTop: 10,
        marginBottom: 10,
        overflowY: "auto"
    },

    welcomeBody: {
        marginTop: 20,
    }
})

export default styles;